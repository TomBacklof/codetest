interface Props {
    childBoxes: {
        left: number[];
        right: number[];
        cols: number[];
        id: string;
    }[];
    cols: number[];
    breakpoints: number[];
    alignX: string[];
    cssMode: "flex" | "grid";
}
export default function getAlignmentXRest({ childBoxes, alignX, cssMode, cols, breakpoints, }: Props): {
    id: string;
    width: number[];
}[];
export {};
