interface Props {
    children: {
        width: number[];
        id: string;
    }[];
    cols: number;
    index: number;
    alignX: string;
}
declare type Row = {
    ids: string[];
    width: number;
};
export default function getBreakpointRows({ index, cols, children, alignX, }: Props): Row[];
export {};
