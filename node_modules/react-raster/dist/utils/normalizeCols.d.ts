interface Props {
    cols?: number | number[];
    parentCols: number[];
    left: number[];
    right: number[];
    breakpoints: number[];
}
export default function normalizeCols({ cols, parentCols, breakpoints, left, right, }: Props): number[];
export {};
