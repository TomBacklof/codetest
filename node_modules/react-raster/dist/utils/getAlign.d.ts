interface Props {
    align: string[];
    cssMode: "grid" | "flex";
    hasChildBoxes: boolean;
}
export default function getAlign({ align, cssMode, hasChildBoxes, }: Props): string[];
export {};
