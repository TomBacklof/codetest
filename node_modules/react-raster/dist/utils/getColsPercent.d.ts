interface Props {
    cols: number[];
    left: number[];
    right: number[];
    parentCols: number[];
    cssMode: "grid" | "flex";
}
export default function getColsPercent({ cols, left, right, parentCols, cssMode, }: Props): number[];
export {};
