interface Props {
    margin: number[];
    cols: number[];
    parentCols: number[];
    cssMode: "grid" | "flex";
    gutterX: string[];
}
export default function getMarginsPercent({ margin, cols, parentCols, cssMode, gutterX, }: Props): (string | undefined)[];
export {};
