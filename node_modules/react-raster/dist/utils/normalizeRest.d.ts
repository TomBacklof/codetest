interface Props {
    rest?: {
        id: string;
        width: number[];
    }[];
    breakpoints: number[];
    id: string;
}
export default function normalizeRest({ rest, id, breakpoints, }: Props): number[];
export {};
