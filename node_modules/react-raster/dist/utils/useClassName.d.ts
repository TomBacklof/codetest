interface Props {
    currentBreakpoint: {
        index: number;
        value: number;
    };
    className?: string;
}
export default function useClassName({ className, currentBreakpoint, }: Props): string;
export {};
