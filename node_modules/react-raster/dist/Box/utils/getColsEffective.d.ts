interface Props {
    cols: number[];
    colspan: number[];
    paddingLeft: number[];
    paddingRight: number[];
}
export default function getColsEffective({ cols, colspan, paddingLeft, paddingRight, }: Props): number[];
export {};
