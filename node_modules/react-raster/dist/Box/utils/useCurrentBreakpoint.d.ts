export declare type currentBreakpoint = {
    index: number;
    value: number;
};
interface Props {
    activateEventListener: boolean;
    contextBreakpoint: currentBreakpoint;
    breakpoints: number[];
}
export default function useCurrentBreakpoint({ activateEventListener, contextBreakpoint, breakpoints, }: Props): currentBreakpoint;
export {};
