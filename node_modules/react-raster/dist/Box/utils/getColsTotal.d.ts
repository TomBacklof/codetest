interface Props {
    cols: number[];
    colspan: number[];
    marginLeft: number[];
    marginRight: number[];
}
export default function getColsTotal({ cols, colspan, marginLeft, marginRight, }: Props): number[];
export {};
