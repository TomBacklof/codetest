interface Props {
    cols?: number | number[];
    colspan: number[];
    marginLeft: number[];
    marginRight: number[];
    paddingLeft?: number[];
    paddingRight?: number[];
    breakpoints: number[];
}
export default function normalizeCols({ cols, colspan, breakpoints, marginLeft, marginRight, paddingLeft, paddingRight, }: Props): number[];
export {};
